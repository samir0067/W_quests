{% extends 'base.html.twig' %}

{% block content %}
    <div class="row">
        {% for product in products %}
            <div class="col-4" style="border: 1px solid black">
                <p>{{ product }}</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}
